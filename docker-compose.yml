version: '3.8'

services:

  api:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: dfu-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./server:/app
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload

